//Додавання тестів для стартової сторінки Google
describe('Google Homepage Tests', () => {
    it('should open Google homepage', async () => {
      // Test to open Google homepage
    });
  
    it('should have a title "Google"', async () => {
      // Test for title "Google"
    });
  
    it('should display search input field', async () => {
      // Test for the presence of search input field
    });
  
    it('should display "Google Search" button', async () => {
      // Test for the presence of "Google Search" button
    });
  
    it('should display "I am a non-existent button" (negative test)', async () => {
      // Negative test for the presence of non-existent button
    });
  
    it('should display "I am a non-existent link" (negative test)', async () => {
      // Negative test for the presence of non-existent link
    });
  });

  